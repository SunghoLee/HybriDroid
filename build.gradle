def wala = file(project.getProperty("walaDir"))

subprojects{
    ext {
        wala_path = wala.getAbsolutePath()
        wala_version = "1.5.3"
    }
}

wrapper{
  ext {
    wala_path = wala.getAbsolutePath()
    wala_version = "1.5.3"
  }
}
