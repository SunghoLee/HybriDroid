def wala = file(project.getProperty("walaDir"))

subprojects{
    ext {
        wala_path = wala.getAbsolutePath()
        wala_version = "1.5.3"
    }
}

wrapper{
  gradleVersion = "5.2.1"
  ext {
    wala_path = wala.getAbsolutePath()
    wala_version = "1.5.3"
  }
}
